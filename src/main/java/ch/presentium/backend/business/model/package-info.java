/**
 * This package should contain the business model classes. All entities below this package should be meant to be, or
 * used apart of, the persistence layer. None of the classes below should be used in API responses or requests.
 *
 * <p>The only link between the business model and the API should be the mappers, converting to view models for responses
 * and converting to / updating the business model using request bodies.
 */
package ch.presentium.backend.business.model;
